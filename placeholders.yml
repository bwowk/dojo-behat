default:
    placeholders:
        mongodb:
        two: "${service}"
        bla:
          $default:
            $modelA: ldap
            $modelB: "${two}"
        api_url:
            $default: localhost:8080
            $dev: 172.32.3.111:8080
